name: login_bank_flow
description: Fluxo de login de um app bancário ficticio

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^4.3.4
  intl: ^0.17.0

dev_dependencies:

flutter:
  uses-material-design: true